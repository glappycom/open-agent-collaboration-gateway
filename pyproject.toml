[project]
name = "oacg"
version = "0.3.0"
description = "Open Agent Collaboration Gateway for bounded, auditable collaboration between heterogeneous AI systems."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "Glappy Inc."}]
keywords = ["ai", "agents", "multi-agent", "openai", "grok", "orchestration", "collaboration", "ai-governance"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
